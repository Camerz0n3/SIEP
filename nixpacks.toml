[phases.setup]
nixPkgs = ["nodejs_23"]

[phases.install]
cmds = ["npm install", "cd dashboard && npm install"]

[phases.build]
cmds = ["npm run build", "cd dashboard && npm run build"]

[start]
cmd = "npm run start"
