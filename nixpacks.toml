[phases.setup]
nixPkgs = ["nodejs_22", "python3", "gcc", "pkg-config"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build", "cd dashboard && npm ci && npm run build"]

[start]
cmd = "npm run start"
